# 感染症マップ 仕様書（v0.1 / PoCベース）

## 1. 目的

国内の感染症発生動向（定点当たり報告数）を、都道府県レベルおよび可能な範囲で保健所管区レベルまで可視化するWebサービスを構築する。各自治体が個別に公開しているデータソース（CSV / HTML / PDF / 自然文 / 画像）の形式差を吸収し、統一スキーマで蓄積・地図表示することを目指す。

本書はPoCで得られた知見（データソース調査結果、抽出方式の検討）をもとに、実装に向けた仕様をまとめたものである。

## 2. 全体アーキテクチャ

5層構成とする。

```
① Raw層（生データ保管）
   → PDF/HTML/CSV/Excelを取得した「そのまま」を保存

② Extract層（pandas / LLM）
   → Raw層を読み、共通スキーマのレコードに変換

③ Validation層（pandera / pydantic）
   → 保健所名・疾患名のenumチェック、数値の妥当性チェック
   → 通過したものは④へ、弾かれたものは人間レビュー用キューへ

④ Store層（DB、時系列蓄積）
   → 検証済みレコードのみを蓄積

⑤ Map層（表示）
   → DBを直接参照せず、薄いAPI層経由で地図に描画
```

### 2.1 各層の責務

| 層 | 責務 | 備考 |
|---|---|---|
| Raw | 取得データの原本保存 | 自治体×週単位でパーティション。再現性・障害調査のため必須 |
| Extract | 原本 → 共通スキーマへの変換 | ソースの形式によりpandas直パースかLLM抽出かを切り替える（3章参照） |
| Validation | スキーマ・値域チェック | 抽出手段によらず共通ルールを適用。ここを一本化することで新規自治体追加時の実装コストを下げる |
| Store | 正規化済みデータの永続化 | 時系列DB。Map層はここのみを参照 |
| Map | 可視化 | API経由でStore層を参照。DBスキーマ変更の影響をフロントに波及させない |

## 3. Extract層：データソース区分と抽出方針

44都道府県・自治体を調査した結果、データソースは3区分に分類できる。

### 3.1 区分の判定基準

| 区分 | 判定基準 | 抽出手段 |
|---|---|---|
| 直接パース | CSV / Excel / 素のHTML表として配布されている | pandas（CSV/Excel）、BeautifulSoup（HTML表） |
| LLM抽出 | PDFの整った数値表、または自然文に埋め込まれた数値 | PDFページ（画像 or テキスト）をLLMに渡し、既知のenumに従うJSONで返却させる |
| 対象外 | グラフ画像・折れ線グラフのみで数値表記がない | 自動化しない。OCRでの目分量読み取りは精度上不採用 |

正規表現によるパースは、フォーマット変更への耐性が低く、季節・年度で自然文の言い回しが変わりやすいため採用しない。PDF表・自然文はいずれもLLM抽出に一本化する。

### 3.2 調査済み自治体の区分（PoC時点）

| 自治体 | 区分 | 形式 | 備考 |
|---|---|---|---|
| 神奈川県 | 直接パース（実データ確認済み） | CSV（公式配布、疾患ごと・年ごとの別ファイル） | 保健所管区＋センター単位、週次全期間。**実データは患者数(実数)を含まず定点当たり報告数のみ**。当初想定した単純な列構成(年,週,保健所名,疾患名,患者数,定点当たり報告数)とは異なり、疾患1件＝1ファイル・週が列のワイド形式だった |
| 千葉市 | 直接パース | CSV（bodik.jp経由） | 地域情報プラットフォーム経由 |
| 北九州市 | 直接パース | CSV（bodik.jp経由） | 同上 |
| 京都府 | 直接パース（実データ確認済み） | CSV（発生地図ページからリンクされた全疾患集計データ、Shift_JIS） | 保健所管区別（乙訓・山城北・山城南・南丹・中丹西・中丹東・丹後）＋**京都市内は行政区ではなく保健所管区5区分**（北・左京／上京・中京・下京／東山・山科／南・伏見／右京・西京）。当初想定した「素のHTML表」は地図ページ内の小さい表(1疾患のみ)で、同ページからリンクされたCSVの方が全疾患を機械的に取得しやすかった |
| 沖縄県 | 直接パース | Excel配布 | 保健所別・年齢別の内訳あり |
| 徳島県 | 直接パース（要確認） | オープンデータ表記だが実体PDFの可能性あり | 個別確認要 |
| 茨城県 | LLM抽出 | PDFグリッド表 | 疾患×保健所の純粋な数値表、自然文なし |
| 宮城県 | LLM抽出 | PDFグリッド表 | 同上 |
| 石川県 | LLM抽出 | PDF表＋矢印記号 | 増減記号（↑↓→）の除去が必要 |
| 静岡市 | LLM抽出 | PDF規則的数値行 | 「報告」「定当」の行が規則的 |
| 東京都 | LLM抽出（要検証） | オープンデータAPI＋グラフ画像 | 保健所別数値がAPI内に含まれるか要確認 |
| 埼玉県 | LLM抽出（限定的） | COVID期のみCSV、通常疾患はPDF/グラフ | 恒常運用データではない |
| 兵庫県 | LLM抽出（限定的） | 表形式（COVIDページは2024年3月更新終了） | 現行運用の代替ソース要確認 |
| 広島県 | LLM抽出（要確認） | 整った県全体集計表 | 保健所別内訳の有無が未確認 |
| 大阪市 | LLM抽出 | 自然文埋め込み | 「北部7.00」のような表記 |
| 北海道 | LLM抽出 | 保健所ごと個別PDF | 統一CSVなし、保健所数分のページ回収が必要 |
| 福島県 | 対象外（管内別のみ） | 総評文は良質だが管内別数値はグラフ画像 | 県全体の定点数など一部のみ表 |
| 愛知県 | 対象外 | 定点マップは画像、本文はクリニック雑感 | 構造化データがほぼ存在しない |
| 栃木県 | 対象外 | 折れ線グラフ画像のみ | 数値表記なし |

#### 3.2.1 追加調査分（要実ページ確認）

以下は残り32都道府県について調査エージェントがWeb検索で追加調査した結果。**この調査を行った実行環境ではネットワークegressプロキシが各自治体の公式ドメイン（`*.lg.jp`等）へのアクセスを一律ブロックしており、実際にページ・PDF・CSVを開いて中身を確認することができなかった。** そのため以下は検索結果のスニペット（タイトル・抜粋文・ファイル名）のみからの推測であり、特に「保健所別内訳の有無」「PDF内が表形式かグラフのみか」「HTML表かPDF埋め込みか」は断定できていない項目が多い。**実装（パーサ追加）に着手する前に、制限のない環境からの実ページ確認が必須。**

| 自治体 | 区分（推定） | 形式 | 備考 |
|---|---|---|---|
| 青森県 | LLM抽出 | PDF週報 | 保健所別の定点表＋自然文の警報記述が混在。URL: pref.aomori.lg.jp配下のwr*.pdf |
| 岩手県 | 未確認 | 不明 | グラフのみ（対象外）の可能性あり。要実ページ確認 |
| 秋田県 | 直接パース（暫定） | HTML表の可能性＋PDF併存 | `idsc.pref.akita.jp/kss/week_tei.asp`が動的HTML表の可能性。保健所（福祉環境事務所）別内訳ありのPDF（RAPIDS.pdf）も確認 |
| 山形県 | LLM抽出 | PDF週報 | 保健所別（山形市・村山・最上・置賜等）記載ありとの情報 |
| 群馬県 | LLM抽出 | PDF | 整った数値表あり。保健所別内訳の有無は未確認 |
| 千葉県 | LLM抽出 | PDF（＋提出用Excel様式） | 疾患別・保健所別5週グラフPDFの存在から保健所別データが示唆されるが表かグラフか未確認 |
| 新潟県 | LLM抽出 | PDF週報 | 保健所別添付資料への言及あり |
| 富山県 | LLM抽出 | PDF | 厚生センター別推移グラフ＋報告数一覧表の記載あり |
| 福井県 | 直接パース（推定） | Excel（保健所別定点あたり患者数推移） | 過去データはExcelダウンロード可能との記載。実ページ未確認 |
| 山梨県 | 直接パース（確認済み・実装済み） | CSV（オープンデータカタログ/CKAN系） | `catalog.dataplatform-yamanashi.jp`から実CSVを取得・確認済み。**保健所別内訳は無く県全体集計のみ**（region=ALL）。疾患×{累積(週別実数)/定当/推移/状況}のワイド形式。`backend/app/extract/yamanashi.py`実装済み、`backend/fixtures/yamanashi_sample.csv`は実データ(2026年1〜5週) |
| 長野県 | LLM抽出（推定） | PDF週報 | 保健所（保健福祉事務所）単位のページにもPDF配布 |
| 岐阜県 | LLM抽出（推定） | PDF週報 | 保健所別・市町村別内訳の有無は未確認 |
| 静岡県 | LLM抽出（推定） | PDF | 「保健所別定点把握感染症集計表」の名称あり、保健所別内訳を含む可能性が高い |
| 三重県 | 未確認 | 不明（HTML表かPDFか未確認） | 「IDWR Mie」週別アーカイブページあり |
| 滋賀県 | LLM抽出（推定） | PDF | 地域医師会サイトにも転載。保健所別内訳未確認 |
| 岡山県 | LLM抽出（推定） | PDF（毎週金曜更新） | 県内7地域（保健所）別に集計との記述あり |
| 大阪府 | 未確認 | 不明 | 大阪府感染症情報センター週報。ブロック別・年齢別発生状況の記載はあるが配布形式未確認 |
| 奈良県 | LLM抽出 | PDF | 実PDF確認済み。北部・中部・南部および保健所別内訳あり |
| 和歌山県 | LLM抽出 | PDF | 実PDF確認済み（WIDR）。県全体及び保健所別の定点当たり患者報告数の記載あり |
| 鳥取県 | 未確認 | 不明 | 週報本体の形式は検索結果からは断定不可 |
| 島根県 | 直接パース（確認済み） | 素のHTML表 | 独自の感染症情報提供システム（NESID系, didss.dsvc.jp）。実ページ確認により、weeklyページの中身は素のHTMLテーブルと判明。pandas.read_html等で直接パース可能 |
| 山口県 | LLM抽出（確認済み） | PDF（条件指定ダウンロード形式） | 実ページ確認により、opendataページは条件（週・疾患等）を指定してPDFを生成・ダウンロードする方式と判明。CSVではなくPDFなので直接パース不可。PDF内が規則的な表であればLLM抽出、グラフのみなら対象外の可能性あり（PDF中身は未確認） |
| 香川県 | LLM抽出 | PDF | 過去号PDF多数確認。保健所別内訳の有無は未確認 |
| 愛媛県 | 未確認（LLM抽出寄り推定） | 不明 | 定点あたり報告数の記載はあるが配布形式未確認 |
| 高知県 | LLM抽出（推測） | PDF | 県全体集計＋上位5疾患の推移表。保健所別内訳未確認 |
| 福岡県 | 直接パース寄り（未確定） | HTML表の可能性 | 福岡県保健環境研究所サイトに保健所別5週分の週報ページ（idwr-f2.html）を確認。マークアップ未確認 |
| 佐賀県 | 未確認 | 不明（動的Webシステム） | 島根・山口と同系統のNESID型システム。CSV/Excel配布ページの存在は確認したが中身未確認 |
| 長崎県 | LLM抽出 | PDF | 保健所別内訳を含む旨の記述あり |
| 熊本県 | LLM抽出 | PDF | 熊本市は別途PDFでバックナンバー公開。保健所別内訳未確認 |
| 大分県 | 直接パース寄り（未確定） | Excel/CSV＋PDF混在（推定） | 過去データページにExcel形式の配布を確認。通常の週報自体はPDF中心とみられる |
| 宮崎県 | 未確認 | 不明 | 号ごとの個別URL。HTML本文かPDFリンクページか未確認 |
| 鹿児島県 | LLM抽出 | PDF | 年度別週報一覧経由でPDFを確認。保健所別か県全体集計かは未確認 |

※ これで47都道府県全てについて一次調査は完了（うち19自治体は実ページ確認済み、追加32県は検索スニペットのみに基づく暫定分類）。次のアクションは9章参照。

## 4. 共通データスキーマ

Extract層の出力、およびStore層のテーブル定義。

```
infectious_disease_reports
- id                    -- 主キー
- year                  -- 年
- week_number           -- 疫学週番号
- week_start_date       -- 週開始日
- prefecture            -- 都道府県名
- region                -- 保健所名・市区町村名。県全体集計は "ALL"
- disease               -- 疾患名（enum管理、5.1参照）
- patient_count         -- 実患者数（nullable）
- per_sentinel_count    -- 定点当たり報告数（nullable）
- source_tier           -- "direct_parse" | "llm_extract"
- source_url            -- 取得元URL
- extracted_by          -- "pandas" | "llm"
- fetched_at            -- 取得日時
- validation_status     -- "passed" | "flagged"
```

Raw層は上記とは別に、取得したファイル本体を自治体×週単位で保存する（形式は元ファイルのまま：pdf/html/csv/xlsx）。

## 5. Validation層

### 5.1 Enum管理

- `region`：都道府県ごとに保健所・センター・市区町村名のマスタリストを事前定義する。LLM抽出結果がこのリストに存在しない場合は `validation_status = flagged` とし、`region = null` のまま格納しない（推測で近い名前に丸めない）。
- `disease`：定点把握対象疾患（インフルエンザ、COVID-19、RSウイルス感染症、感染性胃腸炎等）のマスタリストを事前定義する。同様に未知の疾患名は弾く。

### 5.2 数値妥当性チェック

- `per_sentinel_count` が負値、または現実的な上限（暫定：200）を超える場合は `flagged`。
- 前週比で10倍以上の急変がある場合は自動通過させず、人間レビューのキューに回す（誤読の可能性が高いため）。
- `patient_count` と `per_sentinel_count` の整合性（定点数が既知の場合は `patient_count / 定点数 ≈ per_sentinel_count`）が取れない場合は `flagged`。

### 5.3 LLM抽出結果への追加ルール

- 抽出プロンプトは必ず「JSON以外を出力しない」「不明な値は null とし、推測しない」を明示する。
- 同一ソースに対し複数回抽出して結果が割れる場合は `flagged` とし、差分を記録する（将来的な精度検証のため）。

## 6. Store層

- 時系列DB（PostgreSQL等を想定）。`validation_status = passed` のレコードのみを正式データとして扱う。
- `flagged` レコードは別テーブルまたは別ステータスで保持し、人間のレビュー後に再投入できるようにする。
- パーティションは `prefecture × year` を基本単位とする。

## 7. Map層

- フロントはStore層に直接クエリを投げず、薄いAPI層を経由する。
- API層の責務はStore層のクエリのラップのみとし、ビジネスロジック（集計・粒度変換等）は極力持たせない。
- 表示粒度は自治体ごとに異なる（都道府県レベルのみ／保健所管区レベルまで）ため、APIレスポンスに `available_granularity` を含め、フロント側で粒度混在をハンドリングする（PoCの「ドリルダウン地図」と同じ考え方）。

### 7.1 疾患セレクター

- ユーザーが疾患（インフルエンザ／COVID-19／手足口病／感染性胃腸炎／RSウイルス感染症等）を切り替えて閲覧できるUIとする。
- デフォルト表示は、京都府の週報が採用している「その週の上位疾患を自動判定して見出しに出す」方式を参考に、`per_sentinel_count` が高い疾患を自動サジェストする案が有力。

### 7.2 色スケールの疾患別正規化（重要）

疾患ごとに `per_sentinel_count` のレンジが大きく異なるため、**全疾患共通の固定スケールは採用しない**。滋賀県の実データ（保健所別・第20週）で検証した結果、以下の通りレンジの差は一桁以上に及ぶ。

| 疾患 | 最小 | 最大 |
|---|---|---|
| 感染性胃腸炎 | 6.00 | 24.50 |
| A群溶連菌咽頭炎 | 0.86 | 6.00 |
| インフルエンザ | 0 | 1.27 |
| 水痘 | 0.25 | 1.60 |
| 手足口病 | 0 | 0.22 |
| RSウイルス感染症 | 0 | 0 |

固定スケールで塗ると、レンジの狭い疾患（手足口病等）が常に真っ白になり、「流行していない」のか「データが無い」のかを利用者が区別できなくなる。そのため以下のルールとする。

- 色スケールは**疾患を切り替えるたびに、その疾患・その週のデータの min/max で再計算する**（API側が `available_granularity` と同様に、疾患ごとの `scale_min` / `scale_max` をレスポンスに含める）。
- 全地域が0（RSウイルス感染症の例のように、当該週にその疾患の報告が全く無い場合）は、薄い色で塗るのではなく**「報告なし」を示す専用パターン（グレーの斜線等）で明示的に区別する**。0を最小値として色を塗ると「少しだけ流行している」ように誤読されるため。
- 全数把握疾患（麻しん等、4章の `report_type = "case_based"`）は定点当たり報告数を持たないため、疾患セレクターで選択した際は塗り分けの基準を `patient_count`（実数）に切り替える、別ロジックとする。

## 8. 運用（Dagster想定）

- Raw取得 → Extract → Validation を asset chain として構成する（`raw_file` → `parsed_records` → `validated_records`）。
- 直接パース対象（P0）は毎週自動実行。
- LLM抽出対象は初期は手動トリガーとし、精度が安定した段階で自動化に移行する。
- 自治体ごとにパーティション化し、個別の失敗が他自治体の処理に波及しないようにする。

## 9. 今後の拡張

- 47都道府県の一次調査は完了（3.2・3.2.1参照）。ただし追加調査した32県分は、egressブロックのため検索スニペットのみに基づく暫定分類であり、**実ページを開いての最終確認が未実施**。次のアクションとして、制限のない環境から以下を優先的に実ページ確認する:
  - 「直接パース」候補（実装コストが低く優先度高）: 秋田県（HTML表の可能性）、福井県（Excel）、山梨県（CSVオープンデータ）、**島根県（実ページ確認済み・素のHTML表、pandas.read_html等で直接パース可能）**、福岡県（HTML表の可能性）、大分県（Excel/CSV混在の可能性）
  - 山口県は実ページ確認の結果、条件指定→PDF生成方式と判明したため「直接パース」から除外し「LLM抽出（要検証）」に区分変更済み。
  - 形式が全く未確認の県（岩手・三重・大阪府・鳥取・愛媛・佐賀・宮崎）は最優先で実ページ確認し、「対象外」に該当しないか見極める。
- 東京都のオープンデータAPI、広島県・徳島県の資料については、保健所別の実数値が含まれるか個別に検証する。
- MVPの対象範囲は「直接パース」区分の自治体（神奈川県・千葉市・北九州市・京都府・沖縄県）に限定し、LLM抽出対象は後続フェーズで拡張する。追加調査で見つかった直接パース候補（島根県・山口県・福井県・山梨県等）は、実ページ確認後にMVP対象へ追加を検討する。

## 10. 未確定事項（要決定）

- LLM抽出のプロンプト詳細（JSONスキーマの厳密な形、few-shot例の要否）
- Validation層で `flagged` になったレコードのレビューフロー（誰が・どこで確認するか）
- Store層のDB選定（PostgreSQL / その他）
- Map層のホスティング先・API層のフレームワーク
